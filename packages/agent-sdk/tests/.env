# Test Environment Configuration

# Test workspace (will be cleaned up after tests)
TEST_WORKSPACE=/tmp/agent-sdk-test

# Agent API Configuration
# For most tests, fake key is fine (agent creation, initialization without real API calls)
# For real API tests (message exchange), put your real key in tests/.env.secret
AGENT_API_KEY=sk-ant-test-key-for-bdd-tests
AGENT_BASE_URL=https://api.anthropic.com

# Test database (SQLite)
TEST_DB_PATH=/tmp/agent-sdk-test/test.db

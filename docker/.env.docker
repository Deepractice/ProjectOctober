# =============================================================================
# Claude Code UI - Docker Environment Configuration
# Only 3 core variables, everything else is fixed
# =============================================================================

# 1. Anthropic API Key (REQUIRED)
# Get from: ~/.claude/settings.json or console.anthropic.com
ANTHROPIC_API_KEY=your_api_key_here

# 2. Anthropic API Base URL (optional)
# Default: https://api.anthropic.com
# Example relay: https://relay.deepractice.ai/api
ANTHROPIC_BASE_URL=https://api.anthropic.com

# 3. Project Path (host side only, used in docker-compose.yml)
# This is where your project is on the HOST machine
# It will be mounted to /project inside the container
# Example: /Users/sean/myproject or /home/user/workspace
PROJECT_PATH=.

# =============================================================================
# Deepractice Agent - Development Environment
# =============================================================================

# =============================================================================
# PORT CONFIGURATION (See docs/port-allocation.md)
# =============================================================================

# Agent service - unified entry point (API + WebSocket + SPA)
PORT=5200

# Note: Vite dev server runs on default port 5173 (no configuration needed)

# =============================================================================
# BACKEND CONFIGURATION (agent-service)
# =============================================================================

# Environment
NODE_ENV=development

# Frontend URL (for CORS - Vite dev server in development)
FRONTEND_URL=http://localhost:5173

# Anthropic API credentials are in env/.env.secret (shared across all environments)
# See env/.env.secret.example for setup instructions

# =============================================================================
# PROJECT CONFIGURATION
# =============================================================================

# Default project path
# Example: PROJECT_PATH=/Users/yourname/projects/myproject
PROJECT_PATH=/Users/sean/Deepractice/projects/Agent/demo

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Custom Claude CLI path (if not using default "claude")
# CLAUDE_CLI_PATH=claude
